# 代码修改错误记录

> 本文件用于记录项目开发 / 联调过程中发现的问题、根因分析与修改方法。
> **规则：每次修改代码后，必须在本文件追加一条记录（问题、根因、修改文件、修改方法、验证结果）。**

---

## 一、修改记录（按时间倒序）

### 2026-08-24 ｜ 大屏筛选失败（500）：Spark 在 Windows 下缺少 HADOOP_HOME（winutils）

- **问题现象**
  后端可正常启动（/health 200），但大屏"应用筛选"后聚合接口返回 500，
  页面图表不刷新；后端日志：
  `java.io.FileNotFoundException: HADOOP_HOME and hadoop.home.dir are unset.`
  且 `health` 显示 `loaded: false`（数据从未加载成功）。

- **根因分析**
  Spark 在 Windows 上创建本地临时目录/执行 chmod 依赖 Hadoop 的
  `winutils.exe` + `hadoop.dll`，需要 `HADOOP_HOME` 指向其所在目录。
  本机从未安装该二进制，且后端 `app/utils/spark.py` 的
  `build_spark_session` 只处理了 JAVA_HOME / PYSPARK_PYTHON，未处理 HADOOP_HOME。
  首次聚合触发 SparkContext 初始化即失败 → 数据加载失败 → 聚合 500 → 筛选失败。
  （附：本机 `D:\Coding\Java\jdk-1.7` 目录实际为 JDK 17.0.12，JAVA_HOME 配置正确，
  非本次问题。）

- **修改文件**
  - 新增 `D:\Project_env\hadoop-bin\bin\{winutils.exe, hadoop.dll}`
    （hadoop-3.3.6 版，从 cdarlint/winutils 仓库下载）
  - `data_process/app/utils/spark.py`
    （新增 `_ensure_hadoop_home()` 探测并注入 HADOOP_HOME）

- **修改方法**
  1. 下载 winutils.exe / hadoop.dll 到 `D:\Project_env\hadoop-bin\bin\`（标准结构）；
  2. `_ensure_hadoop_home()`：Windows 下依次探测环境变量 HADOOP_HOME、
     候选路径（D:\Project_env\hadoop-bin / C:\hadoop / D:\hadoop）中
     是否存在 `bin\winutils.exe`；
  3. `build_spark_session` 在创建 Session 前注入 `HADOOP_HOME`、
     `hadoop.home.dir`，并把 `bin` 目录加入 PATH（供 hadoop.dll 加载），
     与 JAVA_HOME 处理并列；找不到时仅警告（不阻断，便于其他平台）。

- **验证结果**
  ✅ py_compile 通过；探测逻辑确认命中 `D:\Project_env\hadoop-bin`。
  待用户重启后端（Ctrl+C → python run.py）后验证聚合接口恢复 200。

---

### 2026-08-24 ｜ 后端启动失败：app.core.exceptions 缺少 ConflictError / TooManyRequestsError

- **问题现象**
  执行 `python run.py` 启动后端时抛 ImportError：
  `cannot import name 'ConflictError' from 'app.core.exceptions'`，
  启动在注册蓝图阶段（`app.application.memory` 导入）即中断。

- **根因分析**
  队友二期代码（多轮会话 / AI 分析模块）在 `app/application/memory.py` 与
  `app/application/service.py` 中导入 `ConflictError`、`TooManyRequestsError`，
  但 `app/core/exceptions.py` 只有 `RateLimitError`（429）没有这两个类；
  且 `app/core/error_codes.py` 缺少 `CONFLICT = 409` 错误码。
  exceptions 体系与使用方不同步。

- **修改文件**
  - `data_process/app/core/error_codes.py`（新增 `CONFLICT = 409` 及默认消息）
  - `data_process/app/core/exceptions.py`
    （新增 `ConflictError`（409）与 `TooManyRequestsError`（429）两个 BizException 子类）

- **修改方法**
  1. `ConflictError`：`ErrorCode.CONFLICT`（409），用于会话/快照版本冲突；
  2. `TooManyRequestsError`：`ErrorCode.TOO_MANY_REQUESTS`（429），
     用于并发锁等待超时（与 RateLimitError 语义一致但为独立类，满足 memory.py 导入）；
  3. 中间件 `_handle_biz` 按 `e.code` 映射 HTTP 状态码，409/429 自动生效，无需额外处理。

- **验证结果**
  ✅ 语法编译通过（py_compile）；待用户在有 flask 的 analytics 环境重新运行
  `python run.py` 验证完整启动。

---

### 2026-08-24 ｜ 可视化大屏整页深色化（消除浅色页面背景的"白色边框"观感）

- **需求背景**
  大屏页内部图表/面板已是深色主题，但页面外层（body / main-shell）仍为浅色
  `#f4f7ff`，深色卡片四周露出浅色背景并叠加圆角边框，观感像"白色边框"；
  顶部大标题也是深色文字，在深色页面上看不清。

- **根因分析**
  - `body { background: var(--page-bg) }`，`--page-bg: #f4f7ff` 为浅色，所有路由共用；
  - `.topbar h1` 使用全局深色文字 `var(--text)`；
  - `.screen-shell` 有 `border-radius: 14px`，在浅色背景下形成"卡片浮起"感。

- **修改文件**
  - `smart-medical-frontend/src/router/index.ts`（/screen 路由 meta 增加 `dark: true`）
  - `smart-medical-frontend/src/App.vue`（watch `route.meta.dark`，
    切换 `document.body` 的 `theme-dark-screen` 类，离开大屏自动还原）
  - `smart-medical-frontend/src/style.css`
    （`body.theme-dark-screen` 下背景改深色 `#020617`、标题文字改浅色 `#e2f0ff`、
     大屏容器去圆角）

- **修改方法**
  1. 通过路由 meta 驱动 body 类，而不是在大屏组件里写死，避免影响其他页面；
  2. 深色模式下 `.screen-shell` 去圆角（`border-radius: 0`），页面与背景融为一体；
  3. 大屏内部所有面板/筛选控件本就是深色样式，无需改动。

- **验证结果**
  ✅ 已验证：`npm run build`（vue-tsc + vite）通过；进入大屏整页深色无浅色边框，
  离开大屏（数据总览 / 智能对话）恢复浅色主题。

---

### 2026-08-24 ｜ 顶部栏精简：删除刷新按钮与副标题、放大页面标题

- **需求背景**
  三个页面（数据总览 / 可视化大屏 / 智能对话）共用的顶部栏（App.vue 的 topbar）：
  1. 删除「刷新静态数据」按钮；
  2. 删除页面大标题下的小字副标题（`基于清洗后医疗数据与分析服务接口输出进行展示`）；
  3. 放大页面标题，使其更醒目。

- **修改文件**
  - `smart-medical-frontend/src/App.vue`（topbar 精简为仅剩标题）
  - `smart-medical-frontend/src/style.css`（删除 `.topbar-subtitle`/`.topbar-actions` 样式；
    标题字号 34px → 42px，移动端 28px → 32px；标题置顶对齐、去 margin）

- **修改方法**
  1. App.vue 的 topbar 从「标题 + 小字 + 按钮」结构简化为单独 `<h1>`；
  2. store 的 `loadStaticPayload` action 保留（`init()` 启动加载静态数据仍依赖它），
     仅移除按钮入口，不影响数据初始化流程。

- **验证结果**
  ✅ 已验证：`npm run build`（vue-tsc + vite）通过；全工程无
  `topbar-subtitle`/`topbar-actions` 残留引用。

---

### 2026-08-24 ｜ 总览页图表重构：年龄×性别合并 + 大屏图表迁移

- **需求背景**
  按前端需求调整页面内容分配：
  1. 数据总览页把「年龄结构」与「性别构成」合并为一个柱状图，新增可选性别图例
     （默认全年龄，选男/女分别显示对应性别各年龄段数据）；
  2. 删除总览页「出院去向」图；
  3. 把可视化大屏的「重点县区」图与「Top 医院 3D 立体视图」迁移到数据总览页。

- **关键约束与方案**
  静态汇总 JSON（dashboard-data.json）只有全量年龄分布与性别总数，**没有年龄×性别交叉数据**。
  经确认后端 registry 已注册 `gender`、`age_group` 维度且聚合接口支持多维度分组，因此：
  - 在线模式下，总览页调用 `POST /api/v1/aggregations/run`
    （`dimensions: ['age_group','gender']`）获取真实交叉数据，按所选性别过滤显示；
  - 离线模式下性别选项禁用（按钮置灰 + tooltip 提示），图表降级为仅展示全量年龄分布，
    页面仍完整可用，不违背“总览=静态画像”的定位。

- **修改文件**
  - `smart-medical-frontend/src/types/dashboard.ts`（新增 `AgeGenderStat` 类型）
  - `smart-medical-frontend/src/store/dashboard.ts`（新增 `ageGenderDistribution` 状态
    与 `loadAgeGenderDistribution()` action）
  - `smart-medical-frontend/src/components/ChartCard.vue`（header 增加 `extra` 插槽，
    用于放置性别切换按钮）
  - `smart-medical-frontend/src/views/OverviewView.vue`
    （合并年龄×性别柱状图 + 性别图例；删除出院去向；新增重点县区折线图、
     Top 医院 3D 立体视图；布局重排；watch apiAvailable 自动加载交叉数据）
  - `smart-medical-frontend/src/views/BigScreenView.vue`
    （删除「重点县区」countyOption 与「Top 医院 3D」buildCubeOption/cubeRef 相关代码；
     概要圆环移至右列，与严重程度并列，保持 2-2-1 布局平衡；页脚文案更新）

- **修改方法**
  1. 合并图：`genderFilter`（全部/Male/Female）+ `ageGenderOption` computed；
     x 轴固定取 `ageDistribution` 年龄段，选男/女时从交叉数据按 `gender + age` 匹配取值；
  2. 迁移图：重点县区（折线+面积，配色改为浅色主题）、Top 医院 3D
     （渐变双柱叠层，数据源 `dashboard.topFacilities` Top8）；
  3. 大屏删除后保留与地图相关的 `hospital3dSource`（点位）与 `countySource`
     （县级填色 + 底部 Top12），不影响筛选联动。

- **验证结果**
  ✅ 已验证：`npm run build`（vue-tsc + vite）通过；大屏无 `cube`/`countyOption` 残留引用；
  在线模式下性别切换读取后端交叉聚合，离线模式自动禁用并回退全量。

---

### 2026-08-24 ｜ 数据总览页与大屏页图表重复/同质化（页面职责未划分）

- **问题现象**
  「数据总览」页与「可视化大屏」页存在重复与同质化：
  1. **支付方式构成**：总览页静态饼图与大屏页筛选联动饼图完全重复（同指标、同图表类型）；
  2. **KPI 指标**：总览页 4 个 KPI 卡（总住院人次/平均住院时长/平均总费用/急诊占比）
     与大屏 KPI 行（总住院人次/急诊占比/平均费用/平均住院天数）展示同一指标集，
     未筛选时两页数值完全相同；
  3. **严重程度维度**：总览页「严重程度×死亡风险」热力图与大屏「病情严重程度统计」条形图
     同源（均来自严重程度数据），只是展示视角不同；
  4. 大屏概要圆环标题「历史住院数据总览」与「数据总览」页名撞名，易混淆；
  5. 大屏页脚文案仍写「纽约五区地图」，实际已是纽约州全州地图（文案过时）。

- **根因分析**
  页面职责未按「是否随筛选变化」划分：总览页所有图表读静态 `store.payload`
  （dashboard-data.json，不随筛选变化），大屏所有图表读 `store.screenData`
  （`runScreenQuery` 筛选聚合，随筛选变化）。但同一指标（支付方式、KPI、严重程度）
  被两个页面同时展示，静态版与动态版信息重复，造成用户困惑。

- **修改文件**
  - `smart-medical-frontend/src/views/OverviewView.vue`
  - `smart-medical-frontend/src/views/BigScreenView.vue`

- **修改方法**
  1. **总览页只保留不随筛选变化的静态画像**：
     - 删除「支付方式构成」饼图（大屏已有筛选联动动态版，保留大屏版）；
     - 新增两个大屏没有的静态画像图：**性别构成**（环图）、**出院去向 Top 6**（条形图），
       补足总览页内容量，全部来自静态 payload、不随筛选变化；
     - 保留：年龄结构、重点病种 Top 6、严重程度×死亡风险热力图（均为全量画像）；
     - 严重程度热力图改为全宽展示（`chart-grid--single`），矩阵信息更易读；
     - 页脚新增说明「本页为全量静态画像，不随筛选条件变化；筛选分析请前往可视化大屏」。
  2. **大屏页只保留随筛选变化的图表**（现状已全部接入 `runScreenQuery`，符合原则，未删图）：
     - 概要圆环标题「历史住院数据总览」→「平均住院天数」，消除与总览页撞名；
     - 页脚文案更新为「纽约州医院分布地图 / Top 医院 3D 立体视图」，并注明
       「本页图表随筛选条件实时变化；全量静态画像请前往数据总览」。

- **验证结果**
  ✅ 已验证：`npm run build`（vue-tsc + vite）通过；总览页无残留
  `paymentOption` 引用；大屏图表数据链路（screenData）未改动，筛选联动不受影响。

---

### 2026-08-24 ｜ 前端可视化进阶图表空白（v-if 包裹图表容器导致初始化时序错误）

- **问题现象**
  新增的可视化进阶图表（纽约五区地图 / 3D 立体视图 / 年龄组动态时序）在页面上**全部空白**，
  后端/静态数据均有值，控制台无 JS 报错。

- **根因分析**
  图表容器 div 使用了 `v-if="数据.length > 0"` 条件渲染：
  - `onMounted` 时数据尚未加载（异步聚合未完成），`v-if` 为 false → **容器 DOM 不存在**；
  - `echarts.init(ref)` 在 `onMounted` 只执行一次，此时 `ref.value === null` → **图表实例未创建**；
  - 数据加载完成后 `v-if` 变 true、容器渲染出来，但 `init` 时机已错过，页面只剩空 div。

- **修改文件**
  - `smart-medical-frontend/src/views/BigScreenView.vue`（含新图表区域）

- **修改方法**
  1. 图表容器**始终渲染**（不使用 `v-if` 包裹），空数据/无数据时改用独立提示区；
  2. 大屏新增「纽约市五区地图 + 县区 Top12」「Top 医院 3D 立体视图」「年龄组动态时序」
     三个进阶图表，容器在 `onMounted` 时一定存在，`echarts.init` 可正常创建实例；
  3. 删除原「高频关键词云（示意）」面板（按需求文档，可视化进阶直接集成在大屏页）。

- **验证结果**
  ✅ 已验证：`npm run build`（vue-tsc + vite）通过；dev server HMR 正常，
  大屏页地图/3D/时序在静态演示模式下均有数据渲染。

---

### 2026-08-24 ｜ 前端大屏并发请求导致后端卡死（数据加载无互斥锁）

- **问题现象**
  后端自启动后，`POST /api/v1/aggregations/run` 单个请求超过 90s 无响应（curl 超时）；
  后端日志显示同一毫秒内涌入 **6 个并发聚合请求**，每个都打印
  `开始从 MySQL 加载数据`（同时触发 Spark JDBC 全量加载 209 万行），Spark local
  模式下互相抢占资源与 MySQL 连接，全部挂起，接口 120s 超时阈值内无法完成。

- **根因分析**
  `data_process/app/data/data_provider.py` 三个 Provider（CSV / MySQL / HDFS）的
  `dataframe()` 均为无锁惰性加载：
  ```python
  def dataframe(self) -> DataFrame:
      if self._df is None:
          self._load()      # 无锁！并发请求会同时进入并各自执行全量加载
      return self._df
  ```
  前端大屏（BigScreenView.vue）打开时会并行请求多个图表数据，首次访问
  `_df is None`，多个请求同时触发 200 万行全表加载 → 互相竞争卡死。
  单请求测试（26.6s 成功）无法暴露该问题，只有并发访问才触发。

- **修改文件**
  `data_process/app/data/data_provider.py`

- **修改方法**
  为三个 Provider 增加实例级 `threading.Lock()`，`dataframe()` 改为
  **双重检查锁（double-checked locking）**：
  ```python
  self._lock = threading.Lock()

  def dataframe(self) -> DataFrame:
      if self._df is None:
          with self._lock:
              if self._df is None:   # 只允许一个请求执行 _load，其余等待复用
                  self._load()
      return self._df
  ```
  首个请求独占加载，其余请求阻塞等待后直接复用同一个 DataFrame；
  同时顺带保护了 `_get_or_create_spark` 的并发创建。

- **验证结果**
  ⏳ 待验证：重启后端后并发请求应只触发一次加载，其余请求命中缓存。

---

### 2026-08-24 ｜ 后端启动失败：5000 端口被残留进程占用 + MySQL/Redis 未启动

- **问题现象**
  运行 `python run.py` 启动后端时，Flask 绑定 5000 端口失败，进程启动即退出；
  同时 MySQL（3306）与 Redis（6379）均未监听，数据源请求必然失败。

- **根因分析**
  1. **环境目录改名**：`D:\项目环境依赖` 已被改名为 `D:\Project_env`（摆脱中文路径），
     但 `D:\Project_env\start-db.bat` 仍引用旧路径 `subst Z: "D:\项目环境依赖"`，
     subst 映射失败 → 双击脚本时 MySQL/Redis 实际未启动；
  2. **端口被占**：上一次启动的 `python run.py` 进程（PID 57660）未正常退出，
     仍监听 5000 端口，新实例绑定冲突直接退出；
  3. 附：`start-db.bat` 内含中文注释，文件为 UTF-8 编码，cmd（GBK 代码页）解析错乱，
     报 `'click' 不是内部或外部命令` 等误报（不影响功能，但输出混乱）。

- **修改文件**
  - `D:\Project_env\start-db.bat`（环境脚本，非项目代码）

- **修改方法**
  1. 【环境】`Stop-Process` 清理残留后端进程（PID 57660），释放 5000 端口；
  2. 【环境】以新路径直接启动 MySQL 与 Redis：
     - `D:\Project_env\mysql-8.0.46-winx64\bin\mysqld.exe`（`--basedir`/`--datadir` 均指向新路径）
     - `D:\Project_env\Redis-8.6.3-Windows-x64-cygwin-with-Service\redis-server.exe`
  3. 【脚本】重写 `start-db.bat`：全部路径改为 `D:\Project_env`，删除 `subst` 依赖
     （目录已为纯英文，无需虚拟盘符）；注释全部改为 ASCII 避免 cmd/GBK 编码错乱。

- **验证结果**
  ✅ 已验证：MySQL `SELECT VERSION()` 返回 8.0.46，Redis `PONG`；
  后端启动日志 `缓存后端: Redis(127.0.0.1:6379/0)`，`health.status=ok` 且 `data_source=mysql://127.0.0.1:3306/medical_analytics/sparcs_discharge_2021`；
  `POST /api/v1/aggregations/run` 首次聚合 **26.6s** 成功返回 5 家医院真实数据（2,094,484 行），
  第二次 `cached=true`，`query_time` 0.03s。

---

### 2026-08-23 ｜ MySQL 数据源加载超时/失败（Spark JDBC 单分区全表扫描）

- **问题现象**
  后端切换 `data_source=mysql` 后，`POST /api/v1/aggregations/run` 返回 500：
  `Spark 聚合计算失败，请检查参数后重试`，后端日志显示耗时 **234s** 且触发慢查询告警（阈值 5s），接口超时阈值 120s 被突破。

- **根因分析**
  1. 缺少 MySQL Connector/J 驱动 jar：`ClassNotFoundException: com.mysql.cj.jdbc.Driver`（Spark 是 JVM，读 MySQL 必须 JDBC 驱动，PyMySQL 只用于 Python 层入库）；
  2. 驱动就位后，`MySQLDataProvider._load` 使用 **Spark JDBC 单分区全表读取** 200 万行大文本数据，耗时约 234s，超过接口超时导致失败；
  3. 本机存在**两个 mysqld 进程**共用同一 datadir（PID 16168 挂起 + PID 37952 服务），导致 Spark 侧连接间歇性被拒（`ConnectionRefusedError 10061`）。

- **修改/处理**
  1. 【环境】复制 `D:\实习\大数据配套工具包\mysql-connector-java-8.0.30.jar` → `D:\Miniconda\envs\analytics\Lib\site-packages\pyspark\jars\`；
  2. 【环境】停止挂起的 mysqld 进程（PID 16168），保留服务实例（PID 37952）；
  3. 【代码】`data_process/app/data/data_provider.py` 的 `MySQLDataProvider._load`：先查询 `MAX(id)` 上界，按自增主键 `id` 分成 **8 个分区** 并行读取（`partitionColumn/numPartitions/lowerBound/upperBound`），显著缩短加载时间；无 `id` 主键的老表自动回退单分区。

- **验证结果**
  ✅ 已验证：分区读取生效，首次聚合查询 **17s** 成功返回（2,094,484 行数据完好），第二次查询 `cached=True` 命中 Redis 缓存

---

### 2026-08-23 ｜ Spark 加载 MySQL 全表内存溢出（Java heap space）

- **问题现象**
  分区读取后聚合查询仍 500，后端日志根因：`java.lang.OutOfMemoryError: Java heap space`（JDBC 读取任务阶段 OOM）。

- **根因分析**
  Spark 默认 `spark.driver.memory=2g`，`MySQLDataProvider._load` 中 `df.cache()` 需要把 210 万行 × 33 字段（含大文本列）驻留 JVM 堆，2g 不够。

- **修改方法**
  `.env` 增加 `ANALYTICS_SPARK_DRIVER_MEMORY=4g`（本机 16G 内存，环境变量覆盖默认值，未改代码）。

- **验证结果**
  ✅ 已验证：聚合查询 17s 成功返回，无 OOM。

---

### 2026-08-23 ｜ Redis 缓存连接失败，自动降级为进程内缓存

- **问题现象**
  后端启动时日志输出：
  ```
  Redis ping 失败: AbstractConnection.__init__() got an unexpected keyword argument 'connect_timeout'
  Redis 连接失败，降级为进程内缓存
  ```
  `GET /api/v1/health` 中 `cache.backend` 显示 `in-memory-ttl-lru`，而非 `redis`。

- **根因分析**
  `data_process/app/core/cache.py` 的 `RedisCacheBackend.__init__` 将参数名 `connect_timeout` 原样传给
  `redis.ConnectionPool(**kwargs)`。redis-py 的连接参数标准名称为 `socket_connect_timeout`，
  `connect_timeout` 不是合法参数，导致 `ConnectionPool` 初始化抛出
  `TypeError`，缓存后端按"连接失败"降级为进程内缓存。
  该问题与 redis-py 版本无关（5.x ~ 8.x 均不接受 `connect_timeout`）。

- **修改文件**
  `data_process/app/core/cache.py`（第 159 行）

- **修改方法**
  ```
  - "connect_timeout": float(connect_timeout),
  + "socket_connect_timeout": float(connect_timeout),
  ```
  即把 Redis 连接超时参数名改为 redis-py 标准名称 `socket_connect_timeout`。

- **验证结果**
  ✅ 已验证：重启后端后日志显示 `缓存后端: Redis(127.0.0.1:6379/0)`，`health.cache.backend = redis` 且 `connected=true`

---

### 2026-08-24 ｜ /assistant/chat 接口必现 500：AIService 未注入 + 系统代理拦截 LLM 请求

- **问题现象**
  `POST /api/v1/assistant/chat` 每次请求均返 500，日志：`'MedicalAssistantService' object has no attribute '_ai_service'`。
  另外修复后 LLM 集成测试报 `Connection error`（curl 直连同一端点正常）。

- **根因分析**
  1. 提交 `1a26ca8` 把 `chat()` 重构为委托 `self._ai_service.execute()`，但构造函数无
     `ai_service` 参数、工厂 `build_application_service` 也未构建注入 → 属性不存在必然 AttributeError；
  2. Windows 注册表残留系统代理 `127.0.0.1:7892`（未运行），openai SDK/httpx 默认读取环境代理，
     连 localhost Ollama 与远程端点都被代理拦截；curl 不读注册表故能直连。

- **修改文件**
  - `data_process/app/application/service.py`：① 构造函数新增 `ai_service=None`；② 工厂构建
    `AIService` 并注入（local 模式复用同一 AggregationService/AlgorithmService 实例）；③ 新增
    `_chat_local_pipeline` 本地回退流水线（未注入 AIService 时供单元测试/离线使用）；④
    `_merge_follow_up` 将重构后分类器的 `freeform_query` 与旧 `unsupported` 同等对待以继承上下文；
  - `data_process/app/ai/summary/llm_client.py`：OpenAI 客户端改用 `httpx.Client(trust_env=False)` 绕过环境代理。

- **验证结果**
  ✅ 全量 pytest 182 通过（含真实 LLM 端到端）；远程 OpenCode Zen 与本地 Ollama 双通道实测返回正常

---

## 二、历史记录汇总表

| 日期 | 问题 | 根因 | 修改文件 | 修改方法 | 验证结果 |
|---|---|---|---|---|---|
| 2026-08-23 | Spark 加载 MySQL 全表 OOM | `spark.driver.memory=2g` 不够驻留 200 万行 | `data_process/.env` | 增加 `ANALYTICS_SPARK_DRIVER_MEMORY=4g` | ✅ 聚合 17s 成功 |
| 2026-08-23 | MySQL 数据源加载超时/失败 | 缺 JDBC 驱动 jar；单分区全表扫描 234s；双 mysqld 进程 | `app/data/data_provider.py` + 环境 | 复制 mysql-connector jar；按 `id` 分 8 分区读取；清理挂起 mysqld | ✅ 聚合 17s 成功 |
| 2026-08-23 | Redis 缓存连接失败，降级为进程内缓存 | `connect_timeout` 非 redis-py 合法参数名 | `data_process/app/core/cache.py` | 改为 `socket_connect_timeout` | ✅ backend=redis，命中正常 |

---

## 三、修改流程约定

1. 修改前先复现/确认问题，记录问题现象；
2. 修改时保持最小改动，不影响其他功能；
3. 修改后在本文件追加记录并填写验证结果；
4. 涉及他人负责模块（如后端组员代码）的修改，本地验证后需与对应人员同步，避免直接覆盖他人工作。
