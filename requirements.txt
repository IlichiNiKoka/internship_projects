# =============================================================
# 医养项目 - 统一依赖清单（数据清洗 + 大数据分析服务 + AI 智能层）
# Python 版本要求：3.12（已用 Python 3.12.10 验证）
# 安装：python -m pip install -r requirements.txt
#
# 基础设施（MySQL / HDFS / Redis）已全部容器化，无需本地安装任何数据库：
#   首次部署见根目录 deploy-infra.bat；日常开发 python run.py 会自动拉起。
# =============================================================

# ---- 数据清洗流水线（pandas 分块读写 210 万行 CSV）----
# 已验证 pandas 3.0.5；numpy 为其传递依赖（代码中亦有直接引用，显式声明）
pandas>=2.0
numpy>=1.26

# ---- Python 3.12 兼容包（PySpark 3.5 仍依赖 distutils）----
setuptools>=60

# ---- Web 服务层 ----
Flask>=3.1
marshmallow>=4.3
python-dotenv>=1.2

# ---- 数据库入库（人员2 · 二期：MySQL 优先，SQLite 兜底）----
PyMySQL>=1.1

# ---- 大数据计算引擎 ----
# Spark 3.5.1 官方支持 Python 3.8~3.12，兼容 Java 8/11/17（推荐 17）
pyspark==3.5.1
# PyArrow：Spark createDataFrame 的 Arrow 加速路径必需；
# 缺失时会退化为 PythonRDD 路径（Windows 下 worker 不可靠且慢）
pyarrow>=14.0
# 注意：MySQL JDBC 驱动无需手动下载 —— app/utils/spark.py 已通过
# spark.jars.packages=com.mysql:mysql-connector-j:8.4.0 自动解析，
# 仅首次启动需要联网（从 Maven Central 下载并缓存到 ~/.ivy2）。

# ---- 结果缓存 + 多轮会话持久化 ----
# 基础设施为 Docker 容器 medical-redis；未安装本包时自动降级进程内缓存
redis>=5.0,<7.0

# ---- AI 智能层（人员4：意图识别 / 文本生成）----
# OpenAI 兼容协议：DeepSeek / 本地 Ollama / vLLM 等端点均可接入
openai>=1.30
# langchain-core：AI 应用编排 Tool 注册与会话记忆（人员1）
langchain-core>=0.3,<2.0

# ---- 测试 ----
pytest==9.1.1

# ---- 可选依赖（默认安装；缺失时对应优化路径会自动降级，不影响运行）----
# scipy：Windows 下 Spark 原生 BLAS 探测增强（app/utils/spark.py）
scipy>=1.11
# pywin32：Windows 短路径回退优化（同上）
pywin32>=306; sys_platform == "win32"
# gunicorn：生产部署（Linux）wsgi.py 启动方式所需
gunicorn>=23; sys_platform != "win32"
